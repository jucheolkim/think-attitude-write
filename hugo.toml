baseURL = 'https://think-attitude-write.netlify.app/'
languageCode = 'ko-kr'
title = 'think-attitude-write'
theme = 'PaperMod'

[params]
  description = "매시,매일마다 바뀌는 생각,감정을 기록하고 읽는다"
  author = "우주소년"
  defaultTheme = "auto"
  
  # -------------------------------------------
  # 👇 [추가된 설정] 카테고리 보이게 하는 마법의 스위치
  # -------------------------------------------
  ShowBreadCrumbs = true     # 글 제목 위에 'Home > 에세이 > 글제목' 경로 표시
  ShowPostNavLinks = true    # 글 하단에 이전 글/다음 글 버튼 표시
  
  [params.homeInfoParams]
    Title = "안녕하세요! 👋"
    Content = "내 생각,감정의 흐름이 어떻게 움직이나?"

[[menu.main]]
  name = "글 목록"
  url = "/posts/"
  weight = 1

[[menu.main]]
  name = "태그"
  url = "/tags/"
  weight = 2

# -------------------------------------------
# 👇 [추가된 메뉴] 상단에 '카테고리' 버튼 만들기
# -------------------------------------------
[[menu.main]]
  name = "카테고리"
  url = "/categories/"
  weight = 3